# บันทึกเหตุการณ์ด้านความปลอดภัยและการแก้ไข

เอกสารนี้บันทึกกรณีที่รหัสผ่านถูก commit ลง git history ของ repository สาธารณะนี้
พร้อมทั้งการแก้ไขที่ดำเนินการไปแล้ว และเหตุผลเบื้องหลังการตัดสินใจแต่ละข้อ

| หัวข้อ | รายละเอียด |
| --- | --- |
| วันที่ตรวจพบ | 2026-09-20 |
| วันที่แก้ไขเสร็จ | 2026-09-20 |
| ประเภท | Hardcoded credentials in version control |
| สถานะปัจจุบัน | แก้ไขแล้ว — credential ที่รั่วถูกเพิกถอนทั้งหมด |

## 1. สรุปเหตุการณ์

ในช่วงเริ่มต้นของโปรเจ็ค ไฟล์ `docker-compose` ถูกเขียนโดยใส่รหัสผ่านลงไปตรงๆ ในไฟล์
แทนที่จะอ้างอิงผ่านตัวแปรสภาพแวดล้อม เมื่อไฟล์เหล่านี้ถูก commit และ push
รหัสผ่านจึงถูกบันทึกลง git history ของ repository ซึ่งเปิดเป็นสาธารณะมาตั้งแต่ 2026-07-13

รหัสผ่านที่รั่วคือค่า `123456` ซึ่งเป็นรหัสที่อ่อนแออย่างยิ่งและปรากฏอยู่ในลิสต์รหัสผ่าน
ที่ถูกใช้โจมตีแบบ dictionary attack มากที่สุดในโลก ความรุนแรงจึงสูงกว่ากรณีรหัสรั่วทั่วไป
เพราะต่อให้ไม่มีใครอ่าน git history ตัวรหัสเองก็ถูกเดาได้อยู่ดี

## 2. ขอบเขตของข้อมูลที่รั่ว

ตัวแปรที่ถูกเปิดเผย ทั้งสามตัวใช้ค่าเดียวกันคือ `123456`

- `POSTGRES_PASSWORD` — รหัสผ่านผู้ใช้ฐานข้อมูล PostgreSQL
- `PGADMIN_DEFAULT_PASSWORD` — รหัสผ่านเข้าใช้งาน pgAdmin
- `DATABASE_PASSWORD` — รหัสผ่านที่ Strapi ใช้เชื่อมต่อฐานข้อมูล

commit ที่ได้รับผลกระทบ 9 รายการ จากทั้งหมด 31 commits

| Commit | วันที่ | ไฟล์ |
| --- | --- | --- |
| `c142aef` | 2026-08-09 | `docker-compose.ymal` |
| `bc22e5f` | 2026-08-09 | `docker-compose.ymal` |
| `7376d37` | 2026-08-02 | `docker-compose.yaml` |
| `8b64c3e` | 2026-08-02 | `docker-compose.yaml` |
| `0125f9a` | 2026-08-02 | `docker-compose.yaml` |
| `d0ad931` | 2026-08-02 | `docker-compose.yaml` |
| `aa44310` | 2026-07-19 | `docker-compose.yaml` |
| `9f91e7e` | 2026-07-19 | `docker-compose.yaml` |
| `817b7ad` | 2026-07-19 | `docker-compose.yaml` |

ไฟล์ `docker-compose.ymal` เป็นไฟล์ที่สะกดนามสกุลผิดและถูกลบไปแล้วในภายหลัง
แต่เนื้อหายังคงอยู่ใน history จึงนับรวมไว้ด้วย

## 3. สิ่งที่ไม่ได้รั่ว

- ไฟล์ `.env` ไม่เคยถูก commit เลยแม้แต่ครั้งเดียว ตรวจสอบด้วย `git log --all -- .env` ได้ผลลัพธ์ว่าง
- ไม่มีรหัสผ่านของบัญชีผู้ดูแล Strapi หรือบัญชีผู้ใช้ทดสอบปรากฏใน history
- ณ วันที่ตรวจสอบ repository นี้ยังไม่มี fork (0 forks) จึงยังไม่มีสำเนา history กระจายออกไปที่อื่น

## 4. การประเมินความเสี่ยง

ต้องถือว่าข้อมูลที่ถูก push ขึ้น repository สาธารณะแล้วคือข้อมูลที่ **หลุดถาวร**
เพราะไม่มีทางยืนยันได้ว่ามีใครอ่าน คัดลอก หรือทำ index ไปแล้วหรือไม่
สมมติฐานที่ปลอดภัยคือถือว่าค่านั้นอยู่ในมือบุคคลภายนอกเรียบร้อยแล้ว

ด้วยสมมติฐานนี้ การพยายามลบร่องรอยจึงไม่ใช่มาตรการที่ลดความเสี่ยง
มาตรการที่ลดความเสี่ยงจริงมีเพียงอย่างเดียวคือ **การเพิกถอน credential ที่รั่วให้ใช้งานไม่ได้อีก**
ซึ่งได้ดำเนินการแล้วตามหัวข้อถัดไป

## 5. การแก้ไขที่ดำเนินการแล้ว

### 5.1 เปลี่ยนรหัสผ่านทั้งหมด

รหัสผ่านทุกตัวถูกสร้างใหม่เป็นค่าสุ่มความยาว 28 ตัวอักษร ครบทั้งสี่ character class
และไม่ซ้ำกันระหว่างระบบ เพื่อให้การรั่วของระบบหนึ่งไม่ลุกลามไปยังระบบอื่น

| ระบบ | วิธีเปลี่ยน |
| --- | --- |
| PostgreSQL | `ALTER ROLE` โดยตรงในฐานข้อมูล ไม่ใช่แก้เฉพาะไฟล์ตั้งค่า |
| pgAdmin | `setup.py update-user` |
| Strapi admin | `strapi admin:reset-user-password` |
| Strapi user | `POST /api/auth/change-password` |

การเปลี่ยนรหัสของ PostgreSQL ต้องทำในฐานข้อมูลโดยตรง เพราะค่า `POSTGRES_PASSWORD`
จะถูกนำไปใช้เฉพาะตอนสร้าง data directory ครั้งแรกเท่านั้น
การแก้แค่ในไฟล์ `.env` แล้วรีสตาร์ท container จะไม่เปลี่ยนรหัสจริงในฐานข้อมูล

### 5.2 แทนที่ secret ของ Strapi ที่ติดมากับ Docker image

ระหว่างการตรวจสอบพบช่องโหว่เพิ่มเติมที่ไม่เกี่ยวกับ git history โดยตรง
แต่ร้ายแรงกว่ารหัสผ่านที่รั่ว

image `prawee/strapi` ที่โปรเจ็คนี้ใช้ มีไฟล์ `.env` ฝังอยู่ภายใน ซึ่งกำหนดค่า
`APP_KEYS`, `API_TOKEN_SALT`, `ADMIN_JWT_SECRET`, `TRANSFER_TOKEN_SALT` และ `JWT_SECRET` ไว้
และ `docker-compose.yaml` เดิมไม่ได้กำหนดค่าเหล่านี้ทับ

ผลคือ ใครก็ตามที่ `docker pull` image สาธารณะตัวนี้ จะได้กุญแจสำหรับเซ็น JWT
ชุดเดียวกับที่ระบบนี้ใช้ และสามารถปลอม token เข้ามาในสิทธิ์ผู้ดูแลระบบได้
โดยไม่จำเป็นต้องรู้รหัสผ่านใดๆ เลย ช่องโหว่นี้ทำให้การเปลี่ยนรหัสผ่านเพียงอย่างเดียว
ไม่เพียงพอต่อการปิดช่องทางเข้าถึงระบบ

การแก้ไขคือสร้างค่าสุ่มใหม่ทั้งห้าตัวใส่ใน `.env` และส่งเข้า container
ผ่าน `docker-compose.yaml` เพื่อกำหนดทับค่าที่ติดมากับ image

### 5.3 ลดพื้นที่ที่ถูกโจมตีได้

พอร์ตของ PostgreSQL และ pgAdmin เดิมผูกกับ `0.0.0.0` ทำให้เครื่องอื่นในวงเครือข่ายเดียวกัน
เข้าถึงได้ ซึ่งอันตรายมากเมื่อรวมกับรหัสผ่าน `123456` ปัจจุบันเปลี่ยนเป็นผูกกับ `127.0.0.1`
เพื่อให้เข้าถึงได้จากเครื่องที่รันเท่านั้น

pgAdmin ให้บริการผ่าน HTTP ที่ไม่มี TLS การจำกัดให้เข้าถึงได้เฉพาะ loopback
จึงช่วยลดความเสี่ยงที่รหัสผ่านจะถูกดักอ่านระหว่างทางด้วย

### 5.4 ป้องกันการรั่วซ้ำ

เพิ่ม `.env.backup-*` เข้า `.gitignore` เพราะรูปแบบเดิมที่มีเพียง `.env`
จะไม่ครอบคลุมไฟล์สำรองที่มีรหัสผ่านชุดเก่าอยู่ภายใน

## 6. การตรวจสอบผลการแก้ไข

| รายการทดสอบ | ผลที่ได้ |
| --- | --- |
| เข้าสู่ระบบ Strapi admin และเรียก `/admin/users/me` ด้วยรหัสใหม่ | สำเร็จ (HTTP 200) |
| เข้าสู่ระบบ Strapi user และเรียก `/api/users/me` ด้วยรหัสใหม่ | สำเร็จ (HTTP 200) |
| เข้าสู่ระบบด้วยรหัสผ่านชุดเก่า | ถูกปฏิเสธ (HTTP 400) |
| เชื่อมต่อฐานข้อมูลด้วยรหัสใหม่ผ่าน scram-sha-256 | สำเร็จ |
| เชื่อมต่อฐานข้อมูลด้วยรหัสเก่า `123456` | ถูกปฏิเสธ (authentication failed) |
| เปรียบเทียบ secret ของ Strapi กับค่าที่ฝังมากับ image | ต่างกันครบทั้ง 5 ตัว |

หมายเหตุด้านวิธีทดสอบ: การทดสอบรหัสผ่านฐานข้อมูลต้องเชื่อมต่อผ่าน hostname `db`
ไม่ใช่ `127.0.0.1` เนื่องจาก `pg_hba.conf` ที่มากับ image ของ PostgreSQL
กำหนดกฎ `trust` ไว้สำหรับ loopback ซึ่งอนุญาตให้เชื่อมต่อได้โดยไม่ตรวจรหัสผ่าน
การทดสอบผ่าน loopback จึงให้ผลบวกลวงเสมอ ไม่ว่ารหัสผ่านจะถูกหรือผิด

## 7. เหตุผลที่ไม่เขียน git history ใหม่

ทางเลือกหนึ่งที่พิจารณาคือใช้ `git filter-repo` ลบสตริงออกจาก 9 commits แล้ว force-push
แต่ตัดสินใจไม่ทำ ด้วยเหตุผลดังนี้

**การ force-push ไม่ได้ลบข้อมูลออกจาก GitHub จริง** commit ที่กลายเป็น unreachable
จะยังเปิดดูได้ผ่าน URL ที่ระบุ SHA โดยตรงต่อไปเรื่อยๆ จนกว่าจะติดต่อ GitHub Support
ให้สั่งเก็บกวาดให้ หากทำเพียง force-push จะได้ผลลัพธ์ที่ดูเหมือนสะอาด
ทั้งที่ข้อมูลยังเข้าถึงได้ ซึ่งอันตรายกว่าการรู้ตัวว่าข้อมูลยังอยู่

**ต้นทุนสูงแต่ไม่ได้ลดความเสี่ยง** การเขียน history ใหม่จะทำให้ SHA ของ commit
ตั้งแต่จุดที่แก้เป็นต้นไปเปลี่ยนทั้งหมด กระทบ pull request ที่ merge ไปแล้วทั้ง 6 รายการ
และต้อง force-push ทั้ง 5 branches ในเมื่อ credential ถูกเพิกถอนไปแล้ว
ความเสี่ยงที่ลดลงจากการลบสตริงจึงเท่ากับศูนย์

**history ที่คงไว้มีคุณค่าในการตรวจสอบย้อนหลัง** การเก็บร่องรอยว่าเคยมีข้อผิดพลาดอะไร
และแก้ไขอย่างไร เป็นสิ่งที่การตรวจสอบด้านความปลอดภัยต้องการ มากกว่าประวัติที่ถูกลบจนไม่เหลือร่องรอย

## 8. แนวปฏิบัติสำหรับโปรเจ็คนี้ต่อจากนี้

- ห้ามเขียนค่า credential ลงไฟล์ที่ถูก track ด้วย git ทุกกรณี ให้อ้างอิงผ่านตัวแปรใน `.env` เท่านั้น
- ไฟล์ `env.simple` ใช้เป็นแม่แบบที่แสดงเฉพาะรายชื่อตัวแปร โดยไม่มีค่าจริง
- เมื่อใช้ Docker image จากภายนอก ต้องตรวจสอบว่า image นั้นฝัง secret มาด้วยหรือไม่
  และกำหนดค่าทับเสมอ
- เปิดพอร์ตของบริการที่เป็นโครงสร้างพื้นฐาน เช่น ฐานข้อมูลและเครื่องมือจัดการฐานข้อมูล
  เฉพาะบน `127.0.0.1`
- หาก credential รั่วอีกในอนาคต ให้ถือว่าหลุดถาวรและเริ่มจากการเพิกถอนทันที
  ก่อนพิจารณาเรื่องการลบร่องรอย
